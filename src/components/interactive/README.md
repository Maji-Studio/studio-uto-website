# Interactive Components

Use this folder later for hydrated React islands and Three.js scenes.

The current site stays static by default. Add `client:visible`, `client:idle`, or `client:load` only for components that need browser-side interactivity.
